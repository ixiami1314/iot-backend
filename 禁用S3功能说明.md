# 禁用Amazon S3功能的方法

## 概述
ELADMIN项目集成了Amazon S3协议云存储功能，支持七牛云、阿里云OSS、腾讯云COS、华为云OBS、移动云EOS等。如果您不需要使用S3存储功能，可以通过以下几种方式禁用它。

## 方法1：通过配置文件禁用（推荐）

### 1.1 修改配置文件
在 `application-dev.yml` 或 `application-prod.yml` 中添加：

```yaml
# 亚马逊S3协议云存储配置
amz:
  s3:
    # 是否启用S3存储功能
    enabled: false
    # 其他配置保持不变...
```

### 1.2 配置类修改
已修改 `AmzS3Config.java` 添加条件注解：
```java
@ConditionalOnProperty(name = "amz.s3.enabled", havingValue = "true", matchIfMissing = false)
```

### 1.3 控制器修改
已修改 `S3StorageController.java` 添加条件注解：
```java
@ConditionalOnProperty(name = "amz.s3.enabled", havingValue = "true", matchIfMissing = false)
```

## 方法2：通过Profile禁用

### 2.1 创建无S3的Profile
创建 `application-no-s3.yml` 配置文件：
```yaml
amz:
  s3:
    enabled: false
```

### 2.2 修改启动配置
在 `application.yml` 中修改：
```yaml
spring:
  profiles:
    active: dev,no-s3  # 添加no-s3 profile
```

## 方法3：通过Maven排除依赖

### 3.1 在pom.xml中排除S3依赖
```xml
<dependency>
    <groupId>me.zhengjie</groupId>
    <artifactId>eladmin-tools</artifactId>
    <exclusions>
        <exclusion>
            <groupId>software.amazon.awssdk</groupId>
            <artifactId>s3</artifactId>
        </exclusion>
    </exclusions>
</dependency>
```

## 方法4：通过代码注释禁用

### 4.1 注释S3相关类
- 注释 `AmzS3Config.java` 的 `@Configuration` 注解
- 注释 `S3StorageController.java` 的 `@RestController` 注解
- 注释 `S3StorageService` 相关实现类

## 验证禁用效果

### 1. 启动应用
```bash
mvn spring-boot:run
```

### 2. 检查日志
启动时应该看到类似日志：
```
Skipping bean creation for AmzS3Config due to condition
Skipping bean creation for S3StorageController due to condition
```

### 3. 访问API
尝试访问S3相关API应该返回404：
```
GET http://localhost:8000/api/s3Storage
```

### 4. 检查Swagger文档
在Swagger UI中不应该看到"S3协议云存储管理"相关的API。

## 注意事项

1. **数据迁移**：如果之前使用S3存储了文件，禁用前需要先迁移数据
2. **依赖关系**：确保没有其他模块依赖S3功能
3. **配置一致性**：确保所有环境的配置文件都设置了 `enabled: false`
4. **重启应用**：修改配置后需要重启应用才能生效

## 重新启用S3

如果需要重新启用S3功能：

1. 修改配置文件：
```yaml
amz:
  s3:
    enabled: true
    # 配置正确的S3参数
```

2. 重启应用

3. 验证功能正常

## 推荐方案

**推荐使用方法1（配置文件禁用）**，因为：
- 配置简单，只需要修改一个参数
- 不需要修改代码
- 可以随时重新启用
- 保持了代码的完整性
- 符合Spring Boot的最佳实践
